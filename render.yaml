services:
  - type: web
    name: polymer-similarity-explorer
    runtime: docker
    plan: free
    autoDeployTrigger: commit
    healthCheckPath: /api/health
    envVars:
      - key: CACHE_DIR
        value: demo/cache
      - key: DATASET_PATH
        value: SMILES_Big_Data_Set.csv
